# Deliberately not generalized

This list is load-bearing. It is how this stays a substrate instead of
an “AI operating system”.

- Task tracker and any ticket lifecycle
- Risk tiers and the R1–R4 escalation ladder
- Product premerge, compile, nextest, cook, clippy-vs-main
- Shared GPU locks, GPU doctors, capture scripts
- Live proof / visual goldens
- Deploy / ship / push
- Worktree *creation* and merge/commit/push
- Native Codex / Claude / Grok rails
- Quota broker files — only a hook
- Machine-specific extra read roots as defaults
- Product-specific import/fidelity rules
- Coordinator / manager / stop-hook policy
- Project HANDOFF template sections
- A universal autonomous manager
- Auto-retry of write jobs
- OpenAI models via OpenCode; defaulting native-pool models through this rail
- A dynamic capability/cost/trust broker (schema slots only)
- Dual independently-maintained skill copies per harness
